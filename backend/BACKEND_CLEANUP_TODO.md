# 后端清洗待办

## 供应商模块

- [x] 调整供应商创建流程，不再信任前端传入的 `adminId`。
- [x] 从 `SupplierCreateDto` 删除 `adminId` 及其校验注解。
- [x] 在 `SupplierService.create()` 中通过 `RequestUser.notNull().getRequestId()` 获取当前登录管理员 ID。
- [x] 根据当前管理员 ID 查询 `Admin` 并绑定到新供应商。

## 商品 SKU 模块

- [ ] 完成手动创建 SKU 的前端时，从后端选项值生成规格组合并检查重复；后续视需要在后端增加“同一商品不能存在重复规格”的兜底校验。

## 商品选项值模块

- [ ] 删除选项值前检查是否已被 SKU 规格使用；若已使用则禁止物理删除，或后续改为禁用机制。

## 盘点模块

- [ ] 编写盘点前端和 Controller 时，确定重复扫码的处理规则，并在后端兜底检查同一盘点单中的重复 `skuCode`。

## 销售数据聚合模块

- [X] 编写今日销售、成本、毛利率。
- [X] 编写每月的销售额、成本、毛利率。
- [X] 编写允许用户选择时间计算的销售额、成本、毛利率的接口
- [ ] 编写统计挂单，完成订单的接口，并统计出他们的总价格、成本
- [ ] 编写统计进货各个状态的接口，并统计出他们的总成本

## 权限模块

- [ ] 统计出权限模块的权限分布
- [ ] 翻新权限模块的接口
